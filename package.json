{
  "name": "q-core",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react-sidebar": "^3.0.2"
  },
  "scripts": {
    "dev": "cd q-node-server && yarn dev",
    "install": "cd q-node-server && npm install && cd ../q-react-app && npm install"
  }
}
