module.exports = {
  processMoney: ({ transactions, paybacks }) => new Promise((resolve) => {
    // proccess paybacks
    //   yell if a payback doesn't match to a transaction
    // tag all transactions
    resolve([]);
  }),
};
