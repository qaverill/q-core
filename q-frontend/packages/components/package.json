{
  "name": "@q/components",
  "description": "Custom components for Q",
  "version": "1.0.0",
  "author": "Quinn Averill",
  "license": "Q",
  "main": "build/index.js",
  "files": [
    "build/index.js"
  ],
  "scripts": {
    "build": "cd ./q-components/accounting-data && yarn build && cd ../array-selector && yarn build && cd ../explore-page && yarn build && cd ../loading-spinner && yarn build && cd ../spotify-error-page && yarn build && cd ../time-frame && yarn build && cd ../../ && ../../node_modules/.bin/babel src --out-file build/index.js"
  },
  "peerDependencies": {
    "@babel/cli": "^7.6.4",
    "@babel/core": "^7.6.4",
    "@babel/preset-env": "^7.6.3",
    "@babel/preset-react": "^7.6.3",
    "@babel/plugin-transform-react-jsx": "^7.3.0",
    "react": "^16.9.0"
  }
}
